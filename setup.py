"""Compatibility shim for older tooling.

The package metadata lives in pyproject.toml. This file intentionally does
not import OctoPrint's legacy build helper.
"""

from setuptools import setup


setup()
